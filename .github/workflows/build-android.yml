name: Build Android Core

on:
  workflow_dispatch: # قابلیت اجرای دستی
  push:
    tags:
      - "v*"

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.23' # نسخه پیشنهادی برای 2025

      - name: Setup Java (required for gomobile)
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Setup Android NDK
        uses: android-actions/setup-android@v3
        with:
          cmdline-tools-version: 11076708 # آخرین نسخه کامندلاین تولز

      - name: Install Gomobile
        run: |
          go install golang.org/x/mobile/cmd/gomobile@latest
          echo "$(go env GOPATH)/bin" >> $GITHUB_PATH

      - name: Initialize Gomobile
        run: gomobile init

      - name: Build AAR
        # دستور زیر به صورت خودکار تمام معماری‌های اندروید را می‌سازد
        run: |
          gomobile bind -v -target=android -androidapi 21 -o libbox.aar ./mobile/

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: libbox-aar
          path: libbox.aar

      - name: Create Release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v1
        with:
          files: libbox.aar